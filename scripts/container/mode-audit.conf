# Audit mode: allow every HTTPS tunnel and log it, so you can see which domains a task needs
# before adding them to the allowlist. Nothing is inspected. Use it for one task, then go back.
acl audit_https port 443
ssl_bump splice all
http_access allow CONNECT audit_https
